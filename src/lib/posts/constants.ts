export const POSTS_PER_PAGE = 12;
export const HOME_POSTS_LIMIT = 6;
/** Max series shown in the home strip before falling back to one featured + link. */
export const HOME_SERIES_STRIP_MAX = 3;
/** Sidebar list limits to avoid overflow on long pages. */
export const SIDEBAR_RECENT_LIMIT = 3;
export const SIDEBAR_POPULAR_LIMIT = 4;
/** Hide sidebar "Recent" when total published posts are below this threshold. */
export const MIN_POSTS_FOR_SIDEBAR_RECENT = 5;
